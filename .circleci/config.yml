version: 2.1

orbs:
  cypress: cypress-io/cypress@3.1.2

workflows:
  version: 2
  build:
    jobs:
      - cypress/run:
          cypress-command: npx cypress run --spec "cypress/e2e/Cambaytiger/stage/404.js"
          no_output_timeout: 30m
          post-steps:
            - store_artifacts:
                path: cypress/reports
                destination: test-report
